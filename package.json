{
  "name": "demo_stat",
  "version": "0.0.1",
  "description": "Demo stat",
  "main": "demo.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build; mkdir build; cp src/*.js src/index.html src/style.css build",
    "deploy": "gh-pages -d build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nobus/demo_stat.git"
  },
  "author": "Maks Klimin",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/nobus/demo_stat/issues"
  },
  "homepage": "https://github.com/nobus/demo_stat",
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-airbnb-base": "^11.1.3",
    "eslint-plugin-import": "^2.2.0",
    "gh-pages": "^0.12.0"
  }
}
